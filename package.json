{
  "name": "sistema-alimentacao-escolar",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema Web de Gestão da Alimentação Escolar — planejar, comprar, armazenar, preparar, servir, registrar consumo, analisar.",
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install && npm --prefix . install",
    "setup": "npm --prefix server install && npm --prefix client install",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "seed": "npm --prefix server run seed",
    "start": "npm --prefix server start"
  }
}

